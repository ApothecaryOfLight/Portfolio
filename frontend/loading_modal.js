function launch_loading_modal( waiting_func, callback_func ) {
  //1) Display a transparent layer over the page. Animate.

  //2) Hand promise results of waiting_func to callback_func.

  //3) Hide the trasparent layer.
}
